{
  "name": "logs.moe",
  "version": "1.0.0",
  "description": "Share encrypted logs without leaving terminal.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon --env-file=.env.defaults --env-file=.env main.js",
    "start": "node --env-file=.env.defaults --env-file=.env main.js",
    "build-assets": "esbuild assets/*.mjs assets/*.css --minify --out-extension:.js=.mjs --outdir=assets/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@deno/kv": "0.7.0",
    "ejs": "3.1.10",
    "express": "4.19.2",
    "jsdom": "24.1.0",
    "morgan": "1.10.0",
    "multer": "1.4.5-lts.1",
    "short-unique-id": "5.2.0"
  },
  "devDependencies": {
    "esbuild": "0.21.4",
    "nodemon": "3.1.2"
  }
}
